name: Build Linux x64

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  release:
    types: [created]

jobs:
  build-linux-x64:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
        
    - name: Publish App (Linux x64)
      run: dotnet publish EtherTransfer.UI/EtherTransfer.UI.csproj -c Release -r linux-x64 --self-contained true -o ./publish
      
    - name: Create Archive (Linux x64)
      run: tar -czvf EtherTransfer-linux-x64.tar.gz -C ./publish .
      
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: EtherTransfer-linux-x64
        path: EtherTransfer-linux-x64.tar.gz
